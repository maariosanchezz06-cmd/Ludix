<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/ludix_bg">

    <!-- Título -->
    <TextView
        android:id="@+id/tvTituloExplorar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="EXPLORAR JUEGOS"
        android:textColor="@color/white"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_marginStart="20dp"
        android:layout_marginTop="20dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Chips de filtros -->
    <HorizontalScrollView
        android:id="@+id/scrollFiltros"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:scrollbars="none"
        app:layout_constraintTop_toBottomOf="@id/tvTituloExplorar">

        <com.google.android.material.chip.ChipGroup
            android:id="@+id/chipGroupFiltros"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingHorizontal="20dp"
            app:singleSelection="true"
            app:selectionRequired="true">

            <com.google.android.material.chip.Chip
                android:id="@+id/chipRecientes"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="🕐 Recientes"
                android:textColor="@color/white"
                android:checkable="true"
                android:checked="true"
                app:chipBackgroundColor="@color/chip_background_color"
                app:chipStrokeColor="@color/ludix_cyan"
                app:chipStrokeWidth="1dp"
                app:checkedIconVisible="false" />

            <com.google.android.material.chip.Chip
                android:id="@+id/chipMejorValorados"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="⭐ Mejor valorados"
                android:textColor="@color/white"
                android:checkable="true"
                app:chipBackgroundColor="@color/chip_background_color"
                app:chipStrokeColor="@color/ludix_cyan"
                app:chipStrokeWidth="1dp"
                app:checkedIconVisible="false" />

            <com.google.android.material.chip.Chip
                android:id="@+id/chipMasVotados"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="🔥 Más votados"
                android:textColor="@color/white"
                android:checkable="true"
                app:chipBackgroundColor="@color/chip_background_color"
                app:chipStrokeColor="@color/ludix_cyan"
                app:chipStrokeWidth="1dp"
                app:checkedIconVisible="false" />

            <com.google.android.material.chip.Chip
                android:id="@+id/chipAZ"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="🔤 A-Z"
                android:textColor="@color/white"
                android:checkable="true"
                app:chipBackgroundColor="@color/chip_background_color"
                app:chipStrokeColor="@color/ludix_cyan"
                app:chipStrokeWidth="1dp"
                app:checkedIconVisible="false" />

        </com.google.android.material.chip.ChipGroup>
    </HorizontalScrollView>

    <!-- Lista de juegos -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvJuegosCatalogo"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="16dp"
        android:paddingBottom="80dp"
        android:clipToPadding="false"
        app:layout_constraintTop_toBottomOf="@id/scrollFiltros"
        app:layout_constraintBottom_toBottomOf="parent" />

    <!-- Loading indicator -->
    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="@id/rvJuegosCatalogo"
        app:layout_constraintBottom_toBottomOf="@id/rvJuegosCatalogo"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>